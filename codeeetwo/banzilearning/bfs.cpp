#include <bits/stdc++.h>
using namespace std;
int bp[1000][1000];
int x0,y0;//begin;
typedef pair<int,int> P;
int bfs()
{
    queue<P> que;
    que.push(P(x0,y0));
    bp[x0][y0]=0;
    while(que.size())
    {
        P p= que.front();
        que.pop();
        for(; ;)
        {
            
            if(1);
            //bp[][]=bp[p.first][p.second]+1;
        }
    }
}